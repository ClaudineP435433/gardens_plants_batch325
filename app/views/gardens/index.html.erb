<div class="container">
  <h1>My Garden</h1>
  <ul>
    <% @gardens.each do |garden| %>
    <li>
      <%= link_to garden.name, garden_path(garden), class: 'btn btn-primary' %>
    </li>
    <% end %>
  </ul>


</div>
